Phishing Email Detection ML Project

This project uses Machine Learning (Scikit-learn) to detect whether an email is Phishing or Safe based on email text and URL features.

It uses TF-IDF for text processing and Random Forest for classification. The model outputs accuracy and confusion matrix to evaluate performance.
